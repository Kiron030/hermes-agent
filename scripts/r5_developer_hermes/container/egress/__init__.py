"""R5 Developer-Hermes egress boundary.

``host`` is the launcher-side contract (topology, policy fingerprint,
container wiring). ``broker_entrypoint`` runs inside the broker container and
is never imported on the host.
"""
